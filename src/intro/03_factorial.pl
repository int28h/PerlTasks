/*
Напишите функцию, принимающую целое не отрицательное число и печатующую факториал этого числа.
*/
sub fac {
    my ($n) = @_;
    my ($result) = 1;
    
    foreach(1..$n){
        $result = $result * $_
    }
    
    print($result)
}
